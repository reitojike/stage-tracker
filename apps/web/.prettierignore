.next
node_modules
pnpm-lock.yaml
coverage
playwright-report
test-results
storybook-static

# Mechanically generated by `pnpm run supabase:types` (root). Its drift check
# (scripts/check-supabase-types-drift.mjs) compares raw generator
# output, so this must stay byte-identical to it rather than Prettier-formatted.
src/lib/data/database.types.ts
