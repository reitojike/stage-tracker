# CI/local-only Foundation checkout (see .github/workflows/verify.yml)
.foundation-checkout

# Mechanically generated by `npm run supabase:types`; its exact drift check
# (scripts/check-supabase-types-drift.mjs) compares raw generator output, so
# this must stay byte-identical to that output rather than Prettier-formatted.
src/infrastructure/supabase/database.types.ts
