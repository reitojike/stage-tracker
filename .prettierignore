# CI/local-only Foundation checkout (see .github/workflows/verify.yml)
.foundation-checkout

# Runtime output written by `supabase start`/`supabase db reset`
# (gitignored via supabase/.gitignore); not hand-authored source.
supabase/.temp

# Vendored design handoff artifacts (Claude Design canvases, support.js
# runtime, reference PNGs, issue drafts) copied verbatim from outside the
# repo; see docs/design/README.md. Not hand-authored source, and must stay
# byte-identical to the source snapshot rather than Prettier-formatted.
docs/design/

# Mechanically generated by `npm run supabase:types`; its exact drift check
# (scripts/check-supabase-types-drift.mjs) compares raw generator output, so
# this must stay byte-identical to that output rather than Prettier-formatted.
src/infrastructure/supabase/database.types.ts
