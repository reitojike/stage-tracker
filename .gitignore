# See https://help.github.com/articles/ignoring-files/ for more about ignoring files.

# dependencies
/node_modules
/.pnp
.pnp.js

# testing
/coverage

# next.js
/.next/
/out/

# production
/build

# misc
.DS_Store
*.pem

# debug
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# local env files
.env*.local
.env

# vercel
.vercel

# typescript
*.tsbuildinfo
next-env.d.ts

# local/CI-only Foundation checkout (see .github/workflows/verify.yml)
/.foundation-checkout

*storybook.log
storybook-static

# operator-held catalog import seed data (Issue #73).
# Transcribed from third-party production pages and closer to transaction
# data than to product code; this repository is public, so it stays local.
# See docs/runbooks/catalog-import.md.
/data/catalog-imports/

# operator-held Ticket Opportunity import seed data (Issue #163). Same
# reasoning as /data/catalog-imports/ above - see
# docs/runbooks/ticket-opportunity-import.md.
/data/ticket-imports/
