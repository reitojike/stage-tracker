# 11. マイカレンダー

参照: **12d**

前提: #1〜#8

## 変更

`src/app/calendar/page.tsx` / `_components/MyMonthCalendar` / `_components/MySelectedDayList`

- **ActionRow を撤去**。「個人予定を管理」はマイページへ移す（#13）
- カード面の撤去は #6
- **凡例の語彙を行のバッジに揃える**: 「決まっている」→ **参加する**、「検討中」→ **気になる**。帯の2つ（予定を確保 / 確保しない）はそのまま
- 選択日リストの末尾に **追加行** を置く
  - 「9月13日に予定を追加」（16px `#2f4a7a`）＋ 左に ＋ の線画（15px）
  - 高さ 44px、上下に細罫
  - 日付を初期値にして `/schedule/new` へ
- その日に予定が1件も無いときは、リストのかわりに空状態を出す
  - 「この日の予定はまだありません」＋ **primary** の「9月16日に予定を追加」
  - 初めて使う人が最初に出会うのは空の日なので、ここだけ強く出す
- 参加状態バッジの枠線を外し、「チケット確保済み」を done に（#2）
- 日付範囲の表記を短くする: `2026年9月11日〜2026年9月13日` → **`9月11日〜13日`**。年をまたぐ場合のみ年を出す

参照: TURN 13（13a / 13b / 13c の比較、採用は 13c）

## 確認

- 日を選んでいないときは追加行が出ない
- 12d と突き合わせる
