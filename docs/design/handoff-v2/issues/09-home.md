# 9. ホーム

参照: **12a**

前提: #1〜#8

## 変更

`src/app/(home)/page.module.css` / `_components/HomeDeadlineList` / `_components/HomeUpcomingList`

### 申し込み期限

- 見出し行を `space-between` にして、右に「すべて見る ›」（14px `#2f4a7a`、チケット画面へ）。太罫 2px の下線は見出し行全体に付ける
- カード面の撤去は #6
- **各カードを演目詳細へのリンクにする**。カードの最終行（日付）を `space-between` にして右端に `›`（15px `#737b82`）。日付のみで時刻が無いカードには `›` を付けない（3列目は見切れる位置なので）
- 残り日数バッジは #7 の規則

### 直近の予定

- 日付見出しの色は #5
- 行の構成は現状のまま（時刻 → 演目 → 会場 → バッジ、右端に `›`）
- 参加状態バッジの枠線を外す（#2）
- 出す範囲は #8

## 確認

- 12a と並べて、期限の列が細罫で切れ、3列目が右端で見切れる
- 8月30日（日）が赤、9月2日（水）が墨
