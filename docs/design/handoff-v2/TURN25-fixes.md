# TURN 24 → 25 修正指示

Wave 24 の as built に対する微修正です。**構造の変更ではありません。**
面・角丸・ボタンの強さを TURN 23 の決定稿の語彙に戻すだけです。

正解画像：`reference/*.png`（390px）／運用ルール：`MATERIALIZATION.md`

## 実装のまま採用するもの（決定稿を実装に寄せます）

- 共通 `Sheet.tsx` の切り出し
- シート見出し行 ＋ quiet「閉じる」
- 行の状態ラベル「未定」（決定稿の「未設定」は破棄）

## 修正しないもの

- 24b 参加の状態シート — 既に決定稿と一致。対象外。

---

## 25a イベント詳細 — 2点

参照：`reference/event-detail-390.png` ／ Issue 18

1. **編集の位置と強さ**
   本文中の「この公演情報を編集」（`ActionRow`、細枠ボタン）を削除。
   タイトル右に quiet な「編集」を置く。14px / 補助テキスト色。

2. **公演回リストの面を外す**
   `background: #dfe4e7` / `radius: 12px` のパネルを削除。
   他の一覧と同じ、2px の見出し罫 ＋ 1px の行罫（`padding-block: 12px`）に。
   見出し右に件数「3件」を追加。

## 25b 招待するシート — 2点

参照：`reference/invite-sheet-390.png` ／ Issue 18

中身・文言・挙動は変更なし。角丸のみ。

1. **入力欄** `radius: 10px` → `4px`。文字サイズ 16px、
   ラベルは 12px / 600 ＋ 必須の `*` 表記。
2. **「招待する」ボタン** `radius: 4px` → `2px`。右寄せ。

## 25c 招待一覧 — 3点

参照：`reference/invitations-390.png` ／ Issue 17

1. **白いカードを外す**
   1件ごとの `background: #ffffff` / `radius: 12px` / `gap: 12px` を削除。
   `padding-block: 14px` ＋ 1px の行罫のリストアイテムに。
2. **「参加する」ボタン** `radius: 4px` → `2px`。
3. **中止 badge と件数**
   中止 badge をタイトルの左に並べる。見出し右に「未回答 n件」を追加。
   **n は未回答の件数で、取り消し待ちの行は含めない**
   （参照画像は未回答2件 ＋ 取り消し中1行）。

取り消しトーストの8秒の挙動は変更なし。

---

## 角丸の使い分け（README v2 から更新）

旧「2px と 4px のみ」は粗すぎたため、TURN 23 の実際の使い分けに合わせました。

| 値 | 用途 |
|---|---|
| 2px | バッジ、ボタン |
| 4px | 入力欄、枠、淡い面 |
| 6px | 画面の外枠 |

`background: #ffffff` ＋ `radius: 12px` の白カードは使いません。

## Acceptance Criteria

- [ ] 390px で参照画像との重大な visual 差異がない
- [ ] Issue 17 / 18 の状態表にある全状態を確認した
- [ ] 参照画像から逸脱した箇所を PR 説明に列挙した
