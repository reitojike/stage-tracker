# Stage Tracker リデザイン — 残対応

**このフォルダを見れば、デザイン適用の残りが過不足なく分かります。**
適用済みの経緯は `design_handoff_v2/` に残していますが、着手すべきものは
すべてここに移してあります。

- `RULES.md` — デザインルールの正本（色・角丸・ボタン・シート・バッジ）
- `MATERIALIZATION.md` — 実装の進め方（再現タスク宣言・逸脱の申告義務・受け入れ条件）
- `issues/` — 01〜05 は**すべて適用済み**（#244 / #245 / v3-03 / v3-04 / #248・PR #251）。残りは **06 仕上げ** と **07 書き込みフィードバックの統一** の2本
- `reference/` — 390px の参照画像。**これが正解**

デッキ（`Stage Tracker リデザイン案.dc.html`）の該当フレーム：TURN 23 / 27 / 28 / 29 / 30（実装反映）

## 残対応の全体像

| # | Issue | 対象 | 規模 | 前提 |
|---|---|---|---|---|
| 01 | [共通パーツの語彙を揃える](issues/v3-01-shared-parts.md) | `src/ui/` ＋ 上書き削除4箇所 | **適用済み（#244）** | — |
| 02 | [イベント詳細の中止 badge](issues/v3-02-event-detail-canceled-badge.md) | `EventDetail.module.css` | **適用済み（#245）** | — |
| 03 | [イベントの登録・編集画面](issues/v3-03-event-write-screens.md) | `/catalog/events/new`, `/edit` | **適用済み（2026-08-31 確認）** | — |
| 04 | [個人予定の3画面](issues/v3-04-personal-schedule-screens.md) | `/schedule/[entryId]`, `/new`, `/edit` | **適用済み（2026-08-31 確認）** | — |
| 05 | [サインイン](issues/v3-05-sign-in.md) | `/sign-in` | **適用済み（#248 / PR #251）** | — |
| 06 | [仕上げ](issues/v3-06-finishing-touches.md) | イベント編集 / 共有シート / サインイン | **適用済み（2026-08-31 確認）** | — |
| 07 | [書き込みフィードバックの統一](issues/v3-07-write-feedback.md) | 全書き込み画面 ＋ `src/ui/` | **適用済み（2026-09-01 確認）** | — |
| 08 | [確認シートの footer](issues/v3-08-confirm-sheet-footer.md) | 削除3か所のシート | 小 | 07 |

## 着手順

**残 Issue は0本です。** 01・02 は 2026-08-30 の同期で（as built は TURN 29）、03・04・05 は
2026-08-31 の同期で（as built は TURN 30）適用済みを確認しました。

**05 サインイン（#248 / PR #251、`3823d6b`）：** 本文の重複した `PageHeading` を除去して AppBar の
`STAGE TRACKER` を唯一の page identity に、Passkey を full-width 44px の唯一の filled primary に、
Magic Link を右寄せ 31px の細枠へ。意図的な逸脱1件（参照画像の「サインインリンクを送る」ではなく
既存の「サインインリンクをリクエスト」を維持）は PR 本文で申告済みです。
残るのは `?requested=1` の受付メッセージが `Surface variant="subtle"`（#f7f8f9・角丸12px）のままな点だけで、
これは Issue に含めていなかったぶん。面を外すかどうかは PO 判断。

### 30 で拾った差分（PO 判断待ち・いずれも別 Issue）

0. `?requested=1` の受付メッセージが `Surface variant="subtle"` のまま（05 の対象外）

1. イベント編集画面の下部固定帯は「イベント情報を保存」だけを送る。公演回や中止と削除まで
   スクロールしても同じボタンが出続ける
2. イベント編集画面の公演回行では中止 badge が日時の**下**。詳細画面は #245 で日時の**左**に揃えたばかり
3. 「1件を入力するシート」の形が2つある（公演回＝footer ＋ 閉じるなし / 共有相手を追加＝footer なし ＋ 閉じるあり）。
   どちらも `RULES.md` §7 は満たしている

## 適用済み（このフォルダの対象外）

TURN 12 の8画面、Issue 14〜17、Issue 16（`/schedule` 廃止）、Issue #240（TURN 25 の7点）は
main に入っています。as built は TURN 24 / 26。Issue #244（01）と #245（02）も適用済みで、as built は TURN 29。03・04 の as built は TURN 30。

判断が済んで対応不要になったもの：

- **絞り込みシート** — 実装が共通 `Sheet` に乗ってシンプルになったため、決定稿を実装に寄せた（新しい決定稿は TURN 27b、`reference/filter-sheet-390.png`）
- **チケット画面の状態語** — 旧チケットのドメイン削除に伴う仕様変更。デザイン側の対応は不要
- **未回答の公演回の状態ラベル** — 「未定」が並ぶとノイズになるため、ラベルを出さない実装を採用

## お知らせ（#148）

画面そのものが未実装で、通知トリガー規則・保持期間・既読の持ち方がドメイン未決です。
デザインの残対応ではなくドメインの決めが先なので、この一覧には入れていません。
